[project]
name = "virtual-company"
version = "0.1.0"
description = "ai-driven virtual company"
readme = "README.md"
authors = [
    { name = "Anton Anokhin", email = "anton.anokhin@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.2",
    "anthropic>=1.5.0",
    "asyncpg>=0.31.0",
    "exa-py>=2.20.0",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "greenlet>=3.0",
    "langfuse>=4.15.2",
    "langgraph>=1.2.11",
    "openai>=3.13.0",
    "opentelemetry-api>=1.44.0",
    "opentelemetry-instrumentation-fastapi>=0.65b0",
    "opentelemetry-sdk>=1.44.0",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "sqlalchemy>=2.0.52",
    "tavily-python>=0.8.2",
    "trafilatura>=2.0.0",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
virtual-company = "virtual_company:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.16.6",
]

[tool.pytest.ini_options]
markers = [
    "integration: manually enabled tests that make external provider calls",
]
